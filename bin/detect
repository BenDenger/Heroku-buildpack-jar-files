#!/usr/bin/env bash
# bin/detect <build-dir>

echo $1
if [ -f $1/*.jar ]; then
  echo "Jar File" && exit 0
elif  [ -f $1/build/libs/*.jar ]; then
  echo "found jar in build"
else
    echo "No Jar file found"
fi